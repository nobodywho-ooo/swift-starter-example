import SwiftUI

struct VisionView: View {
    var body: some View {
        NavigationStack {
            Text("Vision")
                .navigationTitle("Vision")
        }
    }
}

#Preview {
    VisionView()
}
