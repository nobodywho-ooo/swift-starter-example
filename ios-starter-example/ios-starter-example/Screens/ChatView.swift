import SwiftUI

struct ChatView: View {
    var body: some View {
        NavigationStack {
            Text("Chat")
                .navigationTitle("Chat")
        }
    }
}

#Preview {
    ChatView()
}
