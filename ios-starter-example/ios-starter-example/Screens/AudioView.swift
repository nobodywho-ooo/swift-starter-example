import SwiftUI

struct AudioView: View {
    var body: some View {
        NavigationStack {
            Text("Audio")
                .navigationTitle("Audio")
        }
    }
}

#Preview {
    AudioView()
}
