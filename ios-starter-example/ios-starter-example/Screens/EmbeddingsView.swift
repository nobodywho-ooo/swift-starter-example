import SwiftUI

struct EmbeddingsView: View {
    var body: some View {
        Text("Embeddings")
            .navigationTitle("Embeddings")
    }
}

#Preview {
    NavigationStack {
        EmbeddingsView()
    }
}
